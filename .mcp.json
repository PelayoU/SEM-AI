{
  "mcpServers": {
    "sem_ai_engine": {
      "command": ".venv/bin/python",
      "args": ["-m", "engine.mcp_server"],
      "env": {
        "SEM_AI_ROOT": "${workspaceFolder}",
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  }
}
