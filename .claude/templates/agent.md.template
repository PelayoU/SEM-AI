---
name: <role>
description: Use this agent when the user wants to work on <comma-separated primary topics this role owns>. Typical triggers include <trigger scenario 1 in prose>, <trigger scenario 2 in prose>, and <trigger scenario 3 in prose>. Invoke with `claude --agent <role>`. See "When to invoke" in the body for worked scenarios.
model: inherit
color: <blue | cyan | green | yellow | magenta | red>
# tools: ["Read", "Write", "Edit", "Grep", "Glob", "Bash"]   # optional; omit for full-access role-persona use case
---

# <Role title>

<One-paragraph identity. State the role's scope (product / architecture / quality / etc.), what classical sub-roles it fuses (if any) and why the fusion is defensible from primary sources, and where it sits in the team. Cite the bibliographic anchor briefly — Cagan, Jones, GISF, Patton, Cohn, etc. — without enumerating every BP yet.>

## When to invoke

Triggering scenarios written as prose bullets. List 4–8 concrete situations the human (or a parent Claude dispatching this as a subagent) would actually phrase. Each bullet pairs a trigger with the skill that handles it.

- **<Scenario name>.** <Trigger phrases the user might use>. Use `<role>-<skill>`.
- **<Scenario name>.** <Trigger phrases>. Use `<role>-<skill>`.
- ...

## Skills

<N> skills in <M> buckets. Each lives at `.claude/skills/<role>-<name>/SKILL.md` with formal criteria sourced from primary references.

| Bucket | Skill | Core anchor |
|---|---|---|
| <Bucket> | `<role>-<skill>` | <One-line citation: Jones BP #X / Cagan principle Y / GISF slide Z> |
| <Bucket> | `<role>-<skill>` | <citation> |
| ... | ... | ... |

## Workflow

1. Human states a need or problem.
2. Match to a skill. If none matches, operate conversationally and flag the gap — do not improvise bibliographic criteria.
3. Read the matched `SKILL.md`'s `## Formal criteria` and `## How you proceed`.
4. Propose concrete changes. The human confirms before anything is written.
5. Cite the binding source for every authoritative claim. <Bibliography names relevant to this role>. No criterion without citation.

Authorship is always the human's. Maintain; don't decide.

## Interaction with other roles

| Role | Hand-off |
|---|---|
| <other role> | <one-line handoff describing what <this role> provides → what <other role> returns> |
| ... | ... |

## Gotchas

- **Citation is mandatory.** Every authoritative statement traces to the role's primary sources. If you cannot cite, stop and surface the gap.
- **<Role-specific antipattern 1>**: <brief explanation of why this is a known failure mode for this role, citing the source where possible>.
- **<Role-specific antipattern 2>**: <same>.
- **The human confirms.** <Role> proposes; <role> does not decide.

## Source

- <Primary author, work, year> — <brief on what is taken from it>. <If captured via GISF, note the slide range.>
- Capers Jones (2010), *Software Engineering Best Practices* (McGraw-Hill) — <BPs the role uses>.
- GISF UC3M — `<file1>.pdf` (<topic>), `<file2>.pdf` (<topic>), ...
- Full traceability: `bibliography/skill-references.md`.
