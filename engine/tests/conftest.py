"""Pytest fixtures shared by the engine tests.

Adapter mocks and complex fixtures land here when the Bloque B tests need them.
For Bloque A (core only) no shared fixtures are required yet — kept as an
empty placeholder.
"""
